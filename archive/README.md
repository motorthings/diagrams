# Archive

Pages kept but no longer listed. Nothing here is linked from a category page.

The files are left in place rather than deleted so an existing URL still resolves
and the reasoning below survives. Moving them out of their category folder was
enough to take them off the site, because every route into them was removed at
the same time.

| File | Came from | Why it is here |
|---|---|---|
| `aesop-hydra.html` | `portfolio/` | Hydra is no longer a mode AESOP runs. The page described functional regression testing across agent versions, and the link to it was removed from the Evaluations group on `aesop.html`. |
| `aesop-hydra-process-map.html` | `aesop/` | The same mode's process map. It had no inbound links even before the move. |

Both sit one level deep, so their `../icon.svg`, `../index.html` and `../aesop.html`
references still resolve. If either is ever restored, move it back to the folder
named above and re-add its row to the Evaluations group on `aesop.html`.

Note that this is a public repo. These pages remain in git history, and deleting a
file here does not remove it from history either.
